import React from 'react';
import { MenuIcon, CloseIcon } from '../Icons';

const Dropdown = () => {
    return (
        <>
            <MenuIcon />
            <CloseIcon />
        </>
    );
};

export default Dropdown;
